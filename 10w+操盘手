一个用于内容创作全流程的 AI Prompt 工具包。**平台无关**——适用于任何支持自定义指令的 AI 工具。

从选题判断到正文生成，帮你把"我想写个东西"变成一篇有结构、有态度、有差异化的成品。

---

## 它能干什么

| 你说           | 它做                      |
| ------------ | ----------------------- |
| "这个选题值不值得写？" | 搜索已有内容，评估差异化空间，给出建议     |
| "帮我拆解一下这个选题" | 设计核心论点、5个备选标题、正文结构、素材清单 |
| "帮我想几个标题"    | 基于选题生成标题，附带策略说明         |
| "帮我写成文章"     | 根据架构生成完整正文，自带自检         |
| "帮我围绕XX写一篇"  | 走完判断→拆解→生成全流程           |

支持公众号和小红书两种平台适配。

---

## 文件说明

```
10w+操盘手/
├── SKILL.md                    ← 核心技能指令（必须）
├── README.md                   ← 你正在看的这个
└── references/
    ├── 内容运营方法论.md         ← 选题和标题方法论（必须）
    └── 风格扩展包-示例.md        ← 写作风格约束（可选，见下文）
```

---

## 在各平台安装

核心思路都一样：**把 SKILL.md 的内容喂给 AI 当系统指令，把 references/ 下的文件当参考资料。**

### CodeBuddy / WorkBuddy

把整个文件夹放到 skills 目录：

```
~/.workbuddy/skills/10w+操盘手/
```

WorkBuddy 会自动识别，对话时触发。

> 如果用 WorkBuddy 版本，建议用公版（带 frontmatter 的版本），功能更完整。

### Claude Code

Claude Code 通过 `CLAUDE.md` 文件加载指令。两种方式：

**方式一：项目级**（推荐）

把 `SKILL.md` 的内容复制到你项目根目录的 `CLAUDE.md` 里（或追加到已有内容后面），把 `references/` 文件夹放到项目根目录。

```
your-project/
├── CLAUDE.md          ← 把 SKILL.md 内容贴进去
└── references/
    ├── 内容运营方法论.md
    └── 风格扩展包.md
```

**方式二：全局级**

把内容放到 `~/.claude/CLAUDE.md`（或 `~/.claude-internal/CLAUDE.md`），全局生效。

### OpenAI Codex CLI

Codex CLI 通过 `AGENTS.md` 文件加载指令：

```
your-project/
├── AGENTS.md          ← 把 SKILL.md 内容贴进去
└── references/
    ├── 内容运营方法论.md
    └── 风格扩展包.md
```

也可以用 `codex.md`（全局配置）或 `--instructions` 参数传入。

### Cursor

Cursor 用 `.cursor/rules/` 目录管理规则：

1. 在项目根目录创建 `.cursor/rules/10w-operator.mdc`
2. 添加 frontmatter 和 SKILL.md 内容：

```markdown
---
description: "内容创作全流程操盘工具"
globs: "**/*.md"
alwaysApply: false
---

（这里贴 SKILL.md 的全部内容）
```

3. 把 `references/` 放到项目根目录

### 其他 AI 工具（ChatGPT、Gemini、通用智能体等）

最朴素的方式：

1. 开一个新对话
2. 把 `SKILL.md` 的全部内容作为第一条消息发送（或放到系统指令/自定义指令里）
3. 如果需要使用风格扩展包，把 `风格扩展包.md` 的内容也一并发送
4. 然后正常对话："帮我写一篇关于XX的文章"

如果平台支持上传文件，直接把 `SKILL.md` 和 `references/` 下的文件都上传。

---

## 关于风格扩展包

这个工具包的核心流程（选题判断、内容拆解、标题创作）和写作风格是**解耦**的。

`references/风格扩展包.md` 是一个**可选的、可替换的**风格层，控制：

- 段落结构和节奏（长短段配比、过渡方式）
- 语感风格（口语化程度、人称、情绪）
- 论证偏好（类比、数据、案例的使用方式）
- AI味黑名单（禁止使用的词汇和句式）
- 自检清单（生成后的逐项检查）

### 三种玩法

**1. 不用风格扩展包**

不提供 `风格扩展包.md`，AI 会自动降级到基础模式——只做信息准确性、逻辑、标题、开头、情绪层次、可读性的基础检查，不约束文风。

**2. 用自带的示例扩展包**

文件夹里附带了 `风格扩展包-示例.md`，是一种有态度、口语化、强类比的写作风格。可以直接重命名为 `风格扩展包.md` 使用，或者参考格式写自己的。

**3. 写自己的风格扩展包**

新建 `references/风格扩展包.md`，按以下章节结构填写：

```markdown
# 风格扩展包

## 一、段落结构
（你的段落配比、长短段要求）

## 二、语感与节奏
（口语化程度、人称、语言风格）

## 三、论证方式
（你偏好的论证手法）

## 四、收尾方式
（结尾风格偏好）

## 五、情绪与态度
（整体情绪基调、立场）

## 六、黑名单
（禁止使用的词汇和句式）

## 七、段落模板
（可选，好段落的示例）

## 八、自检流程
（生成后的检查清单）
```

只要章节结构在，内容随你填。

---

## 核心流程一览

```
判断（选题评估）
  ↓ 搜索已有内容 → 分析角度 → 找差异化 → 输出评估报告
拆解（内容架构）
  ↓ 核心论点 → 标题设计 → 正文结构 → 素材清单
生成（正文写作）
  ↓ 加载风格扩展包 → 搜索补充素材 → 写正文
迭代（自检优化）
  ↓ 按风格扩展包的自检清单逐项检查并修改
```

每一步都可以单独调用，不必走完全流程。

---

## 关于联网搜索

选题评估环节需要搜索已有内容来找差异化角度。不同平台的搜索能力不同：

| 平台                  | 搜索能力           | 建议            |
| ------------------- | -------------- | ------------- |
| CodeBuddy/WorkBuddy | ✅ 内置 WebSearch | 自动搜索          |
| Claude Code         | ✅ 可配置搜索工具      | 确保搜索工具可用      |
| Codex CLI           | ✅ 支持工具调用       | 确保搜索工具可用      |
| ChatGPT Plus        | ✅ 内置联网         | 自动搜索          |
| 无联网的工具              | ❌              | 手动提供相关文章链接或资料 |

如果你用的工具没有搜索能力，在提出选题时附上相关资料即可，不影响后续流程。

---

## 常见问题

**Q：我一定要用命令行工具吗？**
A：不用。ChatGPT网页版、Claude网页版都行，把 SKILL.md 内容贴进去就能用。

**Q：范文PDF是必须的吗？**
A：不是。范文是用于风格对标的参考材料，核心流程不依赖它们。

**Q：英文内容能用吗？**
A：流程和方法论是通用的，但标题技巧和平台适配主要针对中文内容平台。英文使用需要自己调整风格扩展包。

**Q：可以用在小红书上吗？**
A：可以。生成时告诉 AI 目标平台是小红书，会自动调整字数、语言密度和格式。

---

## 致谢

本工具包由 [阿祖](#) 基于多年内容运营实战经验设计，核心方法论来自公众号和小红书的爆款内容复盘。
